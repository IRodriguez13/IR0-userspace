# FASE58 — unified BusyBox ash + coreutils (default interactive build).
# Merges fase50_minimal + extended safe applets.
# Still off: job control, line editing, vi, networking, heavy /proc deps.

CONFIG_BUSYBOX=y
CONFIG_STATIC=y

# Core shell
CONFIG_ASH=y
CONFIG_SH_IS_ASH=y
CONFIG_SH_IS_NONE=n
CONFIG_FEATURE_SH_STANDALONE=y
CONFIG_BASH_IS_NONE=y
CONFIG_ASH_JOB_CONTROL=n
CONFIG_FEATURE_EDITING=n
# Prompt expansion: needed for PS1='user@host:$PWD$ ' in /etc/profile.
# Backslash escapes (\u \h \w) require FEATURE_EDITING_FANCY_PROMPT, still off.
CONFIG_ASH_EXPAND_PRMT=y
# test/[ as ash builtins: '[' has no applet here (CONFIG_TEST1=n) and POSIX
# shell scripts need it.
CONFIG_ASH_TEST=y

# Coreutils — file / directory
CONFIG_CAT=y
CONFIG_ECHO=y
CONFIG_PWD=y
CONFIG_LS=y
CONFIG_MKDIR=y
CONFIG_RMDIR=y
CONFIG_RM=y
CONFIG_CP=y
CONFIG_MV=y
CONFIG_TOUCH=y
CONFIG_LN=y
CONFIG_READLINK=y
CONFIG_STAT=y
CONFIG_CHMOD=y
CONFIG_BASENAME=y
CONFIG_DIRNAME=y

# Coreutils — text / filters (from fase50)
CONFIG_GREP=y
CONFIG_HEAD=y
CONFIG_TAIL=y
CONFIG_WC=y
CONFIG_SORT=y
CONFIG_UNIQ=y

# Coreutils — misc
CONFIG_TEST=y
CONFIG_TRUE=y
CONFIG_FALSE=y
CONFIG_UNAME=y
CONFIG_CLEAR=y
CONFIG_SLEEP=y
CONFIG_PRINTF=y
CONFIG_ENV=y

# Inspection / process / terminal (best-effort)
CONFIG_FIND=y
CONFIG_PS=y
CONFIG_KILL=y
CONFIG_DMESG=y
CONFIG_STTY=y
CONFIG_RESET=y
CONFIG_WHICH=y

# Power / sync (sys_reboot + vfs_sync path)
CONFIG_HALT=y
CONFIG_POWEROFF=y
CONFIG_REBOOT=y
CONFIG_SYNC=y

# Keep off for now
CONFIG_AWK=n
CONFIG_SED=n
CONFIG_VI=n
