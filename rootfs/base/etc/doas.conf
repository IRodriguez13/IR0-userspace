# OpenDoas policy for IR0/Unix (minimal + desktop).
# Owner must be root; mode 0400 or 0440 (doas rejects looser configs).
#
# First-boot puts the initial user in group wheel. That same login password
# authenticates doas. Enable persist when the product rootfs backend supports
# long ticket basenames under /run/doas/.

permit :wheel as root
