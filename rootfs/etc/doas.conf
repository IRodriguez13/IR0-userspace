# OpenDoas policy for IR0 Desktop.
# Owner must be root; mode 0400 or 0440 (doas rejects looser configs).
#
# wheel members may elevate to root after authenticating with their own
# password. Enable persist when the product rootfs backend supports long
# ticket basenames under /run/doas/.

permit :wheel as root
